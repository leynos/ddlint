extends: substitution
message: "Use '-ogue' forms: '%s' → '%s'."
link: "House style: analogue, catalogue (UI 'dialogue' per guide)."
level: error
ignorecase: true
# Word-boundary regexes already enforce whole-word matching; keep nonword false
# to avoid matching partial tokens like "analogous".
nonword: false
swap:
  '\\banalog\\b': analogue
  '\\bcatalog\\b': catalogue
  '\\bdialog\\b': dialogue
