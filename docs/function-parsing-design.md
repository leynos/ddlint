# Function parsing design

This document has been merged into the consolidated parser implementation notes.

Use:

- `docs/parser-implementation-notes.md` for shared parse-utility behaviour,
  delimiter tracking, and declaration parsing invariants.
- `docs/differential-datalog-parser-syntax-spec-updated.md` for normative
  grammar constraints.

Historical function parsing design details are archived in:

- `docs/archive/function-parsing-design.md`
